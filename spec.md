# WRO 2026 場地測量與模擬工具

這是一個網頁版 (Web-based) 應用程式，作為 WRO 2026 場地測量與機器人模擬的工具。

## 1. 場地設定
- **場地內徑尺寸：** 2362mm x 1143mm (單位為 mm)。
- **場地圖檔：** `imgs\WRO-2026-GameMat-Elementary-Printing-File (1)-01.png`。
- **畫面控制：** 
  - 支援滑鼠滾輪進行「等比例放大縮小 (Zoom)」。
  - 支援滑鼠拖曳進行「平移 (Pan)」，類似 Google Maps 的操作體驗。
- **重置功能：** 介面上需提供「重置」按鈕，點擊後清空所有測量線，並將機器人與畫面視角恢復到初始狀態。

## 2. 測量工具
- **基本操作：** 點擊測量工具後，在畫面上點選起點，再點選終點，兩點之間會產生一條線段，並在圖上標示距離 (單位為 mm)。
- **多筆測量：** 畫面上可以同時存在多條測量線。
- **編輯與刪除：**
  - **編輯：** 可以點選現有測量線並拖曳其端點，拖曳過程中會即時重新計算並更新顯示距離。
  - **刪除：** 可以點擊選取某一條測量線後，按下刪除按鈕將其移除。
- **吸附功能 (Snapping)：** 拖曳或點選測量點時，若靠近以下特徵需自動吸附：
  - 場地邊緣。
  - 機器人邊緣。
  - 機器人中心點。

## 3. 機器人設定
- **機器人尺寸：** 寬 208mm，高 215mm。
- **機器人圖檔：** `imgs\國小組比賽機V1_20260504.png`。
- **旋轉中心：** 以圖片左下角為原點 (0,0)，旋轉中心位於 (104mm, 85mm)。機器人在旋轉時，必須以此中心點為軸心，中心點座標在場地上保持不變。
- **初始位置：** 載入時預設放置在場地正中央。
- **操作方式：**
  - **移動：** 可直接拖曳機器人進行移動。
  - **旋轉：** 點擊機器人後會出現「旋轉把手 (Rotation Handle)」，拖曳把手進行旋轉。
    - **快捷鍵限制：** 按住 `Shift` 拖曳時，旋轉角度固定為 90 度的倍數；按住 `Ctrl` 拖曳時，旋轉角度固定為 5 度的倍數。
- **碰撞偵測與吸合：**
  - **碰撞邊界：** 以機器人「旋轉後的真實矩形邊界 (Rotated Bounding Box)」做精確碰撞偵測。機器人無法被移出場地邊緣。
  - **吸合距離：** 當機器人邊緣距離場地邊緣 **5mm 內** 時，會自動吸附並貼齊場地邊緣。